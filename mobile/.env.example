# Public Supabase settings for the local stack: copy this file to `.env` and fill the
# values from the output of `supabase start`. Never put secrets here (no service_role key):
# every EXPO_PUBLIC_* variable is embedded in the app bundle.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
