# mobile/ — Expo + React Native app (`@tripplanner/mobile`)

Read `.claude/skills/mobile-architecture` before adding files; `expo-react-native` before writing them.

## Layout
- `app/` — expo-router routes only (thin). `src/features/<name>/` — feature modules with a public `index.ts`.
- `src/components/` shared UI · `src/lib/` api-client, storage, theme · `src/platform/` ALL platform-specific code.

## Rules
- Backend is Supabase: only `lib/supabase` + feature `api/` modules call `supabase-js`; map rows with `@tripplanner/shared` Zod schemas. Never use the `service_role` key in the app.
- Backend state → TanStack Query. Auth session → LargeSecureStore pattern (AES key in `expo-secure-store`, ciphertext in AsyncStorage: SecureStore's ~2 KB limit is smaller than a Supabase session); other secrets → `expo-secure-store` only (`WHEN_UNLOCKED_THIS_DEVICE_ONLY`; clear on fresh install — iOS Keychain survives reinstall).
- Local storage: AsyncStorage for cache/prefs, `expo-sqlite` for offline trip data. Not MMKV by default (UserDefaults → privacy-manifest risk).
- New dep touching storage/files/device info → check its `PrivacyInfo.xcprivacy` and mirror reasons in `ios.privacyManifests`.
- No `Platform.OS` in feature code; iOS-only modules need an Android fallback in `src/platform/`.
- Request permissions in context with a pre-prompt; every permission needs a usage string in `app.config.ts`.
- Tests: Jest (`jest-expo`) + React Native Testing Library — see `react-native-testing` skill.
- Verify on a real device / release build before calling perf work done.

## Status
Expo SDK 57 app. SPEC-01 / PLAN-01 skeleton (screens S1–S12), theme light/dark/system (one AsyncStorage key), i18n ru/en/uk (i18next; the UI language is a device-local choice in the profile settings card: System | English | Русский | Українська, stored like the theme and applied by `LanguageProvider`; place/airline/currency NAMES have only ru and en, so uk shows the English names via `placeLanguageOf`), `typedRoutes`, privacy manifest mirrored in `app.privacy.ts` (audit: `docs/release/privacy-manifest-audit.md`).
- Real email auth (SPEC-02 / PLAN-02): Supabase client boundary `src/lib/supabase` (the only place that imports `supabase-js`/calls the network; needs `EXPO_PUBLIC_SUPABASE_URL` + `_ANON_KEY` from `.env`, see `.env.example`), LargeSecureStore session in `src/lib/storage`, `src/lib/session` provider (`useSession`) + `Stack.Protected` gating in the root layout, `src/features/auth` screens S2 sign-in / S3 sign-up / S10 forgot password / S10b reset password (route `/reset-password`) validated with `@tripplanner/shared` schemas; profile and tab headers show the real name and email. Apple/Google buttons are still "soon". Not built: account deletion, email confirmation, change password (Follow-ups in SPEC-02).
- Trips are real data (SPEC-03 / PLAN-03; the local `trips` table only, hosted project untouched): TanStack Query via `src/lib/query` (`QueryProvider` in the root layout + `QueryCacheGuard`, which drops the cache when the account ends or changes) and `src/features/trips/api` (the only module that talks to the `trips` table, via `lib/supabase`; queries THROW `TripApiError` carrying a `kind`). Features: `trips` (S4 list, card, status chip, hooks/mutations), `trip-form` (S8 create + S8b edit at `/trips/[tripId]/edit`, own `FormHeader`), `trip-detail` (S7 with the on-screen "..." menu / delete confirmation, "trip not found"), `history` (S5, completed + archived). Today comes from `src/lib/clock` (`useToday`), native dates from `src/platform/datePicker`. There is NO `src/mocks/`: guardrails `no-mocks-directory` and `no-direct-clock-outside-clock` enforce it. The three booking blocks on S7 are still empty placeholders (no flight/hotel/car data or cards yet); the profile row "Connected accounts" carries only the "soon" marker.
- Transport/segments (SPEC-04 / PLAN-04, hosted project untouched): `features/segment-form` (S9 create + S9b edit at `/trips/[tripId]/flights/[flightId]`, flight-number/airport/arrival-whole-or-nothing validation, delete-with-confirmation, "Save and add next" multi-city prefill), `features/transport` (api + hooks over `trip_segments`, `RouteScreen` at `/trips/[tripId]/route`, `TransportBlock` wired into S7), route/gap logic (`buildRoute`) from `@tripplanner/shared`. The `booking-form` `flight` variant/`STATIC_PASSENGERS` stub is gone; the whole `booking-form` feature is now removed (SPEC-07).
- Hotels (SPEC-05 / PLAN-05, local stack only): `features/hotels` (api + hooks over `trip_hotels`, `HotelBlock`/`HotelCard` wired into S7, query errors carry a `kind`), `features/hotel-form` (S14 create at `/trips/[tripId]/hotels/new` + S14b edit at `/trips/[tripId]/hotels/[hotelId]`, in-tree delete/discard overlays, "Hotel not found" state; nights, breakfast range and maps-URL checks come from `@tripplanner/shared`). Amended 2026-09-24: ONE range field "Dates" (`StayDatesField` + `StayRangeCalendar`, copied from trip-form's pattern) plus two optional time fields (`StayTimeField`, never defaulted), the currency is a field-button opening a bottom sheet with search (`CurrencySheet`, names in i18n `hotel.form.currencyName.*`), and S7 cards show calendar dates with the time only when set (`formatCalendarDay`/`formatClockTime`, no zone maths). 2026-09-24 (2): `DismissKeyboardView` (`src/components`) blurs inputs on outside touch, `filterMoneyInput` restricts the cost field, `CurrencySheet` animates via RN `Animated` (`AnimatedSheetOverlay` in `src/components`; reanimated is NOT a dependency). 2026-09-24 (3): the cost input force-re-renders after filtering, and time fields open a bottom sheet on the FIRST tap via `useTimeSheetPicker` (`src/platform/timeSheetPicker.tsx`: iOS in-tree sheet with a native spinner, Android system dialog; value set only on Done; the existing `TimePicker` stays for segment-form). 2026-09-24 (4): the hotel calendar disables past days (create: today; edit: min(today, stored check-in); `hotelDateFloor`, mobile-only `checkIn.inPast` guard), `TextField overlayValue` draws the filtered amount over a transparent native input (no flash of rejected chars), currency placeholder is `EUR` and currency is required when an amount is set. The hotel-stay Maestro e2e flow (PLAN-05 step 10) is NOT authored yet. Known gap: the iOS native compact date/time picker cannot render mono (AC-14).
- Profile (SPEC-06 / PLAN-06, local `profiles` table only): `features/profile` (S6 About-me card, Currency row with value, shared Modal field picker (the city sheet also offers "Add \"typed text\"" for an own city), optimistic serialized writes with rollback, `api/` is the only `profiles` access, `useHomeDefaults` feeds the flight form (first segment: From = home airport) and the hotel form (currency default)). e2e flow `profile-home-airport` written, not yet run.
- Car rental (SPEC-07 / PLAN-07, local `trip_cars` table only): `features/cars` (api + hooks, `CarBlock`/`CarCard` on S7), `features/car-form` (S16b create at `/trips/[tripId]/cars/new` + edit at `/trips/[tripId]/cars/[carId]`, modal), `features/car-view` (S17 at `/trips/[tripId]/cars/[carId]/view`, copy/call actions). New dependency `expo-clipboard` REQUIRES a new dev-client build (`npx expo run:ios`). Return-vs-flight warning from `carReturnAfterFlight` (shared). e2e flow `car-rental` authored blind, never run.
- Works: `pnpm -r typecheck`, `pnpm -r test` (jest-expo, 1613 tests in 129 suites, incl. guardrail tests: route contract, static rules, `backend-only-behind-the-boundary`: `supabase-js`/network only inside `src/lib/supabase`), `npx expo config --type public`. Gotchas: `insights.md`.
- iOS 27 simulator: the CNG `AppDelegate` used to crash at launch (SIGTRAP, UIScene lifecycle). Fixed by the local config plugin `plugins/withIosSceneLifecycle.js` (registered in `app.config.ts`; verified on an iOS 27 sim, then `expo prebuild --clean`); delete it once the SDK template adopts scenes. Details: `insights.md`.
- Repo uses pnpm 11 (`packageManager: pnpm@11.7.0`); `pnpm-workspace.yaml` `allowBuilds` / `minimumReleaseAgeExclude` are temporary and may be deleted later.
