# docs/features/

Human-readable feature docs written by the `doc-writer` agent. Each doc starts with a header stating its
mode (implemented-code / plan-to-docs / arbitrary-material), source, and date. One file per feature: `<slug>.md`.
